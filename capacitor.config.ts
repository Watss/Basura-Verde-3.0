import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Basura Verde',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
